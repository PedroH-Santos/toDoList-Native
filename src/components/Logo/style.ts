import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    
    container: {
        backgroundColor: '#111',
        padding: 60,
    },
    logo: {
        textAlign: "center",
        justifyContent: "center",

    },

})